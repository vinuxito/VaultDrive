# ABRN UI/UX coherence implementation — 2026-09-14

## Objective and scope
Execute all seven roadmap steps through exactly seven distinct implementation iterations on `main`. Baseline `1f42296`. Preserve the six templates, supported crypto formats, working customer data and unrelated edits.

## Intake and files read
Read the requested Filemón philosophy brief, root/frontend README, all seven roadmap step files and index, September lint session/report, current source/tests, Playwright configuration, and Ralph/TDD/Playwright skills. Use Codex-native agents and file-backed progress because no OMX tmux runtime is attached. No separate goal was created.

## Starting state
Unrelated modified `abrndrive`, `.omc/project-memory.json`, `.omc/state/idle-notif-cooldown.json`; untracked `.agents`, `.codegraph`, `.omx`, July shared-folder note and old dist backup. Preserve them; stage only owned files.

## Execution map
Roadmap 01 access actions: iterations 1–3, 4/6 verification. Roadmap 02 transfer outcomes: iterations 2–3/6. Roadmap 03 truthful data/offline states: iterations 1–3/6. Roadmap 04 navigation/localization: iterations 1/2/5. Roadmap 05 onboarding/recovery: iterations 2/3/5. Roadmap 06 trust/help/receipts: iterations 2/5/6. Roadmap 07 release evidence/CI/runbook: all iterations, close in 7. Manual human/device gates are tracked independently; never fabricate them.

## Iteration 1 — reconnaissance and foundation (complete)
Existing reusable foundations: DataState, session vault, full-link creation/recovery, transfer state machines, row action drawers, receipts and locale resources. Fragile seams: nested layout ownership, source failures converted to empty arrays, limited-use streaming/auditing, and old key formats. Smallest safe first slice: truthful source loading plus shell/keyboard foundations and a private verification harness. Baseline: frontend 265 passed/1 skipped; lint exit 0. `go` is unavailable on PATH (exit 126), so the installed recovery toolchain is used explicitly. Initial backend attempt loaded `.env` and failed four database tests because localhost:5433 refused connections (32 pass/4 fail; no successful DB writes). `DB_URL=''` prevents dotenv fallback: 26 pass/6 explicit skips. A private socket-only PostgreSQL 16 cluster was created under this task's `.omx` evidence directory, migrated to schema 49, and all 36 backend tests then passed with its explicit connection string. Added optional LISTEN_HOST to permit a loopback-only integration backend; the existing default bind remains unchanged. Runtime bind smoke is required before closing iteration 1.


### Integration evidence and findings
- Frontend `npm run lint`: exit 0; `npm test`: exit 0, 281 passed/1 existing skip. Private `npm run build -- --outDir .omx/.../iteration-1/dist --emptyOutDir`: exit 0, including `tsc -b`.
- Backend `DB_URL='' go test -count=1 -json ./...`: exit 0 (26 pass/6 DB skips); separate private PostgreSQL schema-49 run: exit 0 (36 pass/0 skip). `go vet ./...`: exit 0.
- Private Go build initially failed VCS stamping due to repository ownership (exit 1). Supplying process-local `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=safe.directory GIT_CONFIG_VALUE_0=/lamp/www/ABRN-Drive` fixed the build (exit 0) without changing global Git configuration.
- Loopback runtime at 127.0.0.1:8091: `/api/healthz` and `/ready` returned 200, readiness schema 49 / synthetic stored files 0. `ss` confirmed loopback-only binding. An initial `/api/readyz` probe was 404; the actual existing endpoint is `/ready`.
- External Playwright on this private runtime: `e2e/theme-visibility.spec.ts --grep 'template selection persists|Spanish phone' --workers=1`, exit 0, 2 passed. The broader initial grep also named download patterns, but matched no download test; no download coverage is claimed for that run.
- New focused tests cover independent unavailable/stale sources, real empty zero, one shell, phone destinations, palette keyboard and hidden feed. CLI external-mode config test passed; absent URL exits 2 as intended. CI now includes lint.
- Review caught a newly declared modal feed without complete focus handling; added initial focus, Tab/Shift+Tab containment, Escape and opener focus restoration. Regression went red then green. Final integrated rerun: 282 frontend tests passed/1 existing skip; lint, TypeScript/private frontend build, backend unit/vet/build and diff check all exit 0. The external-runner target guard remains a recorded iteration-6 hardening item; current browser target is verified private loopback.
- Next lens: implement usable credential-gated outbound links, recoverable transfers, first-use/recovery handoffs and truthful server-stream outcomes. Keep existing crypto formats and production data untouched.

## Iteration 2 — core implementation (complete)
Read iteration 1 findings before changing lens. Its green foundation leaves actual access/transfer completion, first-task guidance and recovery semantics weak. Reuse credential helpers, existing share endpoints, onboarding and Help; keep key formats, production service and customer data unchanged.

Backend test-first evidence: prior code produced 8 successes/access_count=8 for an eight-request one-use race; failed/short streams logged completed downloads; cancelled streams left no outcome; unavailable share inventory returned 200/[]; inactive links invented Revoked. Added deterministic row-lock race and interrupted-writer fixtures. Atomic conditional consumption now enforces active/expiry/unlock/limit at the write, with use consumed on authorized fetch. Byte-count/error-aware auditing labels interrupted streams and survives request cancellation with a bounded context; no JSON is appended to ciphertext. Inventory failures return 503; unknown inactive reason is Closed. Full private-DB suite now 46 passed, 0 skips, exit 0.

Health regression proved DB ping -1 still claimed status ok. It now reports degraded while HTTP200 still establishes API reachability; readiness remains separate. StatusPanel distinguishes responding/degraded/unknown/unreachable, handles malformed data and offers retry, with operator metrics behind admin details and no readiness scans from ordinary pages.


Further iteration-2 evidence: both file/folder revoke endpoints returned 200 for unowned IDs because UPDATE affected-row results were ignored. Red tests reproduced this; atomic owner-scoped closure now returns404, treats repeated owner revocation as already_closed and records one transition. Full private-DB suite: 48 passed/0 skipped, exit0. Stored owner files survive revocation.

Root found an additional live-code seam beyond the initial roadmap recon: Files Quick Share created and copied a bare token URL without a credential gate. It now opens the existing CreateShareLinkModal. A real browser regression failed on the old staged build because no credential field appeared; it will be rerun on iteration2. The first draft selector matched two titles and was corrected before the behavioral red run. Root wired onboarding task state into FirstTaskGuide and existing upload/receive/share actions; guidance distinguishes unknown inventory and actual stored files from button clicks. Focused FirstTaskGuide2 and StatusPanel3 tests pass. Test mock for translation defaultValue was corrected to match i18next behavior; no runtime workaround was added.

### Integrated result and next lens
- Core changes: verified full file/folder link recovery with clipboard fallback; existing Drop-manager handoff; PIN verification before link creation; recoverable public/preview/bulk/ZIP transfers; first-task onboarding and recovery states; shared logout; contextual Help and named route loading/fallback; server one-use enforcement and truthful stream audit.
- `verify-iteration.sh 2`: lint, unit, TypeScript/private frontend build, backend unit/vet/build and whitespace all exit 0. Frontend **324 passed, 0 skipped**; backend explicit-empty-DB **30 passed, 15 intentional DB skips**. Separate private PostgreSQL suite **49 passed, 0 skipped**, exit 0. See `iteration-2/checks.tsv` and command/log files.
- Private runtime switched to iteration-2 artifacts. Playwright Quick Share 1, public-transfer 3 and autofill/PIN 4 regressions: **8 passed**. Owner signup/onboarding/PIN-login initially could not launch the unavailable cached Chromium executable; config now honors an explicit installed-browser path globally (asserted by config verification). Rerun: **1 passed**, exit 0. No production/customer mutation.
- Browser proof exposed existing account/encrypted-key debug output; remove and guard in iteration 6. Iteration-2 code review found no high/critical blocker; revoke audit can be lost on cancellation after mutation, carried explicitly into iteration 3. Failed/short stream classification carried into 6.
- Remaining hardening seams: malformed/session-expiry boundaries; folder-context sharing; independent stale/error data and offline queue retention/reconciliation; ambiguous public uploads; revoke audit atomicity. Preserve crypto formats, server Drop-recovery exception and production configuration. Lane details: `.omx/.../iter2-links.md`, `iter2-transfer.md`, `iter2-guidance.md`.

Additional cold E2E TypeScript check initially exited 2: its old config omitted DOM libraries, and the visual fixture passed reducedMotion outside contextOptions. Corrected both; E2E TypeScript check and scoped ESLint exit 0. App build had already passed because it does not include this separate config. The iteration-2 commit was amended before subsequent work to include the verified correction.

## Iteration 3 — hardening and edge cases (complete)
Read the completed iteration-2 findings. Lens: partial failures, malformed/session state and races. Safest next changes: atomic revoke audit and offline mutations; retained, owner-bound pending work; independent source failures; credential/session guards; ambiguous upload recovery. Preserve existing crypto formats and production/customer state.

### Root hardening evidence (integration pending)
- Red private-DB tests reproduced revoke success without audit, offline rename surviving audit failure, missing action identity/idempotency, and public-info DB outage returned404. Mutation and audit now share SQL transactions; file/folder revocation preserves retryability if recording fails. Sync uses owner/action and file row locks, exact intent fingerprints, action-specific confirmations, 100-action/1MB bounds, and retains supported legacy callers. Current private-DB full suite: **58 passed/0 skipped**, exit0.
- Separate expiry test reproduced HTTP200 after expiry while blocked on a row lock. Claim now locks then checks wall-clock expiry; focused concurrent/expiry/offline/revoke suite with `go test -race` passed (exit0).
- Admin/audit list tests: initial HTTP503/403 no longer appear as empty lists; failed pagination retains history and retries the same offset. Four regressions red then green, exit0.
- SSE tests: late tickets after unmount/auth replacement, missing retries after ticket503 and stale-account401 handling went red then green. Hook exposes live/reconnecting/offline/paused/connecting/signed-out independently from API health. Feed labels session-limited events honestly. Eight focused hook/panel tests pass, exit0.
- An intermediate frontend typecheck encountered active agents' unfinished edits; these are not a final integration verdict. Resolve all errors before iteration commit. New browser state regressions are run against the previous build to establish red, then will be rerun after private iteration3 staging.

### Integration and independent review
- The main integration script passed lint, frontend 372 tests/0 skips, TypeScript/private build, backend unit/vet/build and whitespace. Two additional GroupDetails/IndexedDB abort tests landed afterward; a final frontend suite is running rather than assuming the old count covers them.
- Private staging browser checks: **7 passed**, exit0 (Quick Share, public transfer recovery/missing key/no automatic fetch replay, admin-source retry, visible SSE reconnect, real isolated signup/onboarding/PIN login). The two new state regressions previously failed on iteration2. Separate E2E TypeScript check exit0.
- Readiness caught an outage-test fixture cleanup defect: closing its DB handle left synthetic temporary-file rows. The test now creates no fixtures. Six exact Transfer/Fixture users from the private DB were identified and removed (the reviewer ran the old test once after the first count of five). `/ready` returned200 with schema49 and stored files0 afterward. No customer records were involved.
- Independent backend review approved transaction/idempotency/owner/version/expiry logic with no high/critical issue. Fixed its three medium findings: preserve folder-tree failure cause, return503 for downstream folder-file DB outages, reject hashes exceeding the schema's64-character limit. Added fault-injected downstream and length-bound regressions (all pass). Also removed test-goroutine FailNow by asserting collected outcomes in the test goroutine.
- Auth/link lane:45 focused tests; uploads/request inventory:29; data/offline lane:27; root admin/audit:4 and SSE/feed:9. See `iter3-auth-links.md`, `iter3-uploads.md`, `iter3-data-offline.md` under the raw evidence directory. Unknown post-send uploads are intentionally held for recipient/owner confirmation because the existing API has no immutable client upload receipt lookup; no filename-based acceptance claim is made.
- Next lens: deepen actual journey proof, particularly offline interruption/replay, first-task completion, full file/folder/Drop/request/group bytes, credential reset preserving old files, and negative states. Keep production and crypto contracts untouched; extend the existing test harness rather than add a framework.

Final iteration3 result: frontend **374 passed,0 skipped** across85 files (exit0); private PostgreSQL backend full suite with race detector **63 passed,0 skipped** (exit0); post-review Go vet/build exit0; E2E TypeScript exit0; staged browser7/7; private `/ready`200 after final backend rebuild/restart. No source files changed after the staged frontend build; two added tests were covered by the final unit rerun. Independent backend reviewer: APPROVE, no remaining high/critical/medium finding; its low test-goroutine issue was also fixed. Owned files are the source/test paths in the three lane reports plus root Go hardening/revocation/sync, admin/audit, SSE/feed, the new browser state regression, and this session log.

## Iteration 4 — test depth (complete September16)
Read completed iteration3 findings before changing lens. Test actual upload/share/receive and credential/offline interruption journeys using existing Playwright/Go/Vitest infrastructure. Previous passing component/API tests do not prove full user completion. Add exact-byte and no-duplicate assertions, safe test-target checks, and CI E2E typecheck; fix only reproduced regressions. Preserve production/customer data and encryption formats. Private staging remains 127.0.0.1:8091 with socket-only synthetic PostgreSQL. The full existing browser suite is run alongside independent new credentials/offline/upload proofs; manual participant/device gates remain separate.

### Test-depth discoveries and repairs (integration pending)
- Existing isolated suite exposed stale group-create selectors and onboarding/demo assertions. Scope the group submit to its form; onboarding now chooses a real first task. The recorded demo now verifies a full fragment-bearing share URL and exact recipient bytes. Focused reruns: group/key-prefix2/2 and onboarding/demo2/2 passed. Private backend key-prefix mismatch was corrected in test invocation, not production configuration.
- New upload browser proofs4/4 and offline proofs2/2 passed. They exercise accepted POSTs with lost responses, held unknown outcomes, no blind re-upload, persistent IndexedDB action identity, one audit mutation per replayed intent, conflict retention and filtered-list recovery. Fixture-intercepted failures are identified separately from real isolated writes.
- Actual account recovery exposed the boundary between a recovered RSA key and own files derived from an earlier PIN. Preserve that existing format; add pre-reset/success/download guidance and document original-credential/backup/sender options in `docs/credentials-and-recovery.md`. Supported RSA and unsupported replacement-PIN cohorts are tested separately, with exact original bytes rather than a login-only success claim.
- Actual File Request sender-to-owner download failed: the historical owner access-key field contains a base64 16-byte PBKDF2 salt, not a PIN envelope. Added exact-format compatibility to owner download, preview, verified link recovery and folder key resolution. New real-crypto tests also exposed inconsistent Unicode normalization in the preview worker; it now uses the same NFC normalization as upload. Compatibility tests23/23 passed; the browser red is retained until private staging verification.
- Actual folder-link creation rejected correct version2 PIN envelopes because multiple call sites silently defaulted to version1. Propagate the stored envelope version through folder creation/recovery/collaboration, private-key restoration, shared downloads and preview. Focused regression red2/3 then green7/7; also replace the blank folder error with a useful message. Actual browser green is still required.
- CI now checks both app and E2E TypeScript projects, lint and the external harness configuration; parallel browser lanes have separate artifact directories. A cancellation test proves revoke mutation and audit both roll back if the request is cancelled mid-transaction. Full private PostgreSQL race run64/64 passed, zero skips, exit0. Initial integration lint/unit375/privatebuild/vet/Go-build all passed; later compatibility changes require final checks before this iteration closes.

### Final integration evidence
- Full existing browser baseline:73 passed/11 failed out of84. Ten failures were selector/onboarding/key-prefix harness drift; the remaining mobile row-action failure was a real encryption-proof overlay intercepting taps. The proof is now an inline disclosure. Focused repaired baseline checks: group/key2, demo/onboarding2, all six template share/move/PIN states plus mobile action7; all passed, exit0. No failed baseline case was skipped or deleted.
- Final frontend suite: **387 passed,0 skipped across87 files**, exit0. Lint, app+E2E typecheck, safe harness configuration, private frontend/Go builds, Go vet and whitespace checks exit0. Private PostgreSQL race suite: **64 passed,0 skipped**, exit0. The final ShareModal negative tests reject malformed recipient details and classify first-grant network/5xx outcomes as unconfirmed rather than safe to repeat.
- Direct sharing now accepts the actual API object and verifies decryption before writing any grant. Wrong cached credentials return to manual input; partial group grants identify confirmed members and require access review. Private folder link1/1 and File Request owner download1/1 passed (18.6s/11.7s). Credential sessions1/1 and custodian reset/new-PIN RSA download plus original-PIN legacy download1/1 passed; the recovery case completed in45.8s. The first recovery reruns exposed test assumptions about active login mode and a legitimate post-reload PIN prompt; helpers now select the intended method and answer the visible prompt. Exact bytes remain mandatory.

Next lens findings: navigation vocabulary and role reachability differ; critical EN/ES copy is incomplete; phone/dialog keyboard and zoom handling need a coherent pass. The shared-file PIN dialog has a confirmed filename contrast defect. Trust cards count owner/intake entries as external access, and the legacy “revoke all” label overstates its handler scope. Preserve all six templates and crypto contracts while repairing these seams. Security lifecycle/transaction/proxy/logging findings are recorded for iteration6; no production release claim is made.

### September16 continuation: preview-specific regression
The user supplied a fresh production screenshot: a Secure Drop ZIP rejects PIN decryption in preview. Production index matches the live `vaultdrive_client/dist` bundle (`index-IIdgr0r8.js`), not private iteration4 (`index-BLyBAdDo.js`); service has run since September14 00:30UTC. Private backend8091 had stopped while the synthetic PG cluster remained active; restored backend4 without touching production. Inspection found worker PIN unwrap uses zero salt/IV rather than the stored salt/IV envelope used by the working download path. Add real-crypto and browser ZIP preview byte regressions before repair. A separate red component test shows File Request/password previews incorrectly select account PIN from account settings; reuse the existing file credential classifier. No customer ciphertext, keys or PIN were requested or read.

The preview repair now parses the existing salt/IV/ciphertext envelope, accepts the authenticated owner download-header fallback, and imports backend PKCS#1 RSA keys with RSA-OAEP SHA-256. Real WebCrypto regressions reproduced incorrect Drop decryption, missing RSA hash, PKCS#1 parsing and header fallback failures before repair. File Request preview now selects its original password instead of inferring account PIN from account settings. Fresh full frontend suite: **391 passed across87 files,0 skipped**, exit0; app+E2E typecheck and configuration verification exit0. Final private frontend/Go builds, lint, unit, Go vet and whitespace all exited0. Browser ZIP proof: **1 passed in15.3s**, wrong PIN remains editable, correct PIN enables download, all44 plaintext bytes match (SHA256 `9d9d3cdcdc16df238698e3e5b00e901b9ff97515e2d47c680a8f40851f915d13`). Three earlier browser attempts timed out in login setup; these are harness failures, not product RED. The login helper now awaits the Password-mode button rather than racing lazy route rendering. Product RED is established by real-crypto worker tests. Production remains on its earlier build.

## Iteration 5 — UX/product coherence (complete)
Read iteration4 results, including the corrected Drop/RSA preview and actual exact-byte flows. Apply the user clarity lens: navigation vocabulary, EN/ES critical paths, focus/phone/zoom/contrast, truthful trust counts and revocation receipts remain weak. Reuse semantic tokens, existing route/locale/credential utilities and dialogs; prioritize visible recovery and actions over technical detail. Preserve encryption formats, all six templates, production deployment and customer data. Root owns trust/preview/Files/shared/data-state; bounded native agents own navigation, guidance and transfers.

### User clarity findings and changes
- Root negative tests reproduced rejected revocation becoming false success, owner/intake counted as external read routes, and empty history becoming owner-only assurance. Repaired scope and feedback; added retry and explicit snapshot/unknown states. Reduced repetitive technical cards while preserving access, expiry, counts and history details. Sensitive copy maps to handlers in `docs/trust-copy-contract.md`.
- Preview starts with credentials visible and technical/signing sections collapsed. Shared PIN uses semantic colors, visible inline errors and busy feedback. Folder preview now uses the existing unlocked AES folder key; its real-crypto regression failed on the old PIN branch and passes with exact bytes. Missing folder keys give an explicit reopen-folder action. Original Drop/RSA/password tests remain.
- English/Spanish trust and credential copy, generic empty/error/loading states, future expiry dates and unavailable metadata are explicit. Global test translation stub now matches actual fallback/interpolation and stable function behavior; existing date tests were retained and adapted to locale-aware wording.
- Navigation, guidance and transfer lanes supply independent focused evidence and locale fragments. Full build/browser/contrast integration is still pending.

### Independent review and integration
Initial integrated suite found2 obsolete trust-copy assertions (427passed/2failed); retained contrast coverage and changed expectations to unknown/unavailable rather than false reassurance. Rerun:429passed across92files,0skips; lint/type/privatebuild/Go unit/vet/build/diff all exit0. This is the pre-review checkpoint, not final iteration acceptance. Independent review then caught collaborator folder preview, plaintext signing prompt, worker cancellation, reconstructed timeline dates, confirmed-mutation/stale-refresh wording and active-vs-total counts. Preview repairs are delegated with new regressions; root repaired receipt/count/history semantics. Durable backend history is explicitly carried to6. Final build/browser gates remain pending after these repairs.

### Final preview safety and staged checks
- Independent preview repair added a masked in-app signing PIN dialog, collaborator folder-context propagation and direct-RSA fallback. Cancellation now guards the pending private-key/PIN work, Worker lifecycle and text decoding; stale requests cannot publish results after file change/unmount. New negative tests reproduced each late completion before repair. Focused preview26/26 and related51/51 passed.
- Fresh full frontend suite after those guards: **441 passed across92files,0skips**, exit0. The earlier integrated439 result preceded two additional cancellation tests. Final lint/type/build and visual matrix are tracked separately below.
- Private stage5 served `index-Domh_tl_.js`; readiness200/schema49. Fresh real Drop ZIP preview/download1/1 passed in14.5s with exact-byte comparison. The first invocation also named a nonexistent folder spec; Playwright matched only the Drop test, so no folder result is inferred from it. The actual `coherence-folder-sharing.spec.ts` is run separately.

Browser findings: an initial ambiguous filename/actions selector was corrected to exact matching (harness defect). The new matrix then exposed real raw-English crypto errors in Spanish shared-PIN recovery and hovered outline-button contrast1.43/1.12/1.13 in dark/quantix/cyberpunk. Shared-key unlock and post-unlock failures now map to existing localized recovery messages; malformed-envelope regression5/5 passes with lint/type. The common outline button now uses matching semantic foreground/background and hover-accent pairs, preserving all six palettes. Browser contrast assertions remain unchanged. The actual folder-link ZIP journey passed1/1 in19.9s. Final441 suite/lint/type/build checkpoint passed before these two last visual repairs; rebuilt browser proof follows.

### Iteration5 final verdict
All six skins × EN/ES × desktop1280 and phone390 critical flows passed; final full browser matrix **14/14 in46.0s**, exit0 (`run10-final-full`). Includes the640×400 and195×422 CSS-viewport/DSF2 reflow proxies; these do not establish native browser zoom or physical-device acceptance. The195-width RED (document250px) was corrected by wrapping the shell header/Files toolbar and narrowing preview gutters; measured viewport195/document195, no horizontal overflow. No controls were removed. Screenshot inspection covered light Spanish PIN recovery, dark preview/access, phone command palette and elegant Spanish reflow.

Final frontend suite **442 passed across92files,0skips**, exit0; final narrow class changes then passed16 focused tests and app type/lint. Full app+E2E typecheck, safe runner configuration, private frontend builds, Go unit/vet/build and diff checks exited0. Go source is unchanged from iteration4's64/64 private-DB race proof; this iteration does not claim a fresh DB race run. Final served private bundle `index-2A3c_302.js` in `iteration-5/reflow-fixed-dist`. Real DropZIP1/1 and folder-linkZIP1/1 passed with exact decrypted bytes. Production8082/dist/customer data remain unchanged. Final browser capture refinement uses viewport-only screenshots for fixed dialogs; proxy recapture2/2 passed in5.2s with credential and decrypt action scrolled into view. A disabled decrypt button in the empty-PIN fixture was correctly located semantically rather than as an enabled control. Browser owned ESLint/E2E types/diff all exit0. Source/test manifest: `iteration-5/owned-files.txt`.

Next lens: repair the confirmed credential-version/transaction and recovery authorization boundaries, access aggregation/history and scoped revoke, group permissions, proxy trust/log redaction/stream outcomes and safe support evidence. Fresh read-only security review found public username-only recovery status exposes approved key parts; this is a release blocker and needs request-bound capability/attempt authorization. Preserve existing ciphertext formats and configured custodian envelopes; any additive private migration must have an explicit production release procedure.

## Iteration 6 — security, resilience and observability (in progress)
Read iteration5 final findings and committed proof `7b7530c`. Apply production security/resilience lens: user-facing truth now depends on fresh authorization, atomic credential/revoke changes, durable history and private diagnostics. Confirmed weaknesses include public username-only recovery disclosure/reset, missing group authorization, false owner-only access on DB errors, ignored stream failures, forged proxy identities and raw request/key logging. Implement narrow fullstack repairs with negative tests and a request-bound recovery capability where current architecture requires it. Preserve ciphertext/key envelope formats, configured custodian envelopes, all six themes and unrelated working-tree state. Production service8082/customer DB/frontend remain untouched. Root owns operations/logs/proxy/stream/harness/support integration; agents own credential/recovery, access history and group authorization. Additive migration is applied only to isolated test DB until reviewed release.


### Security integration and review findings
- Implemented request-bound hashed recovery capabilities, attempt/custodian/challenge binding, version-aware atomic password/PIN envelopes, one-use recovery reset with refresh-token revocation and required transactional audit. Migration050 is private-test-only. Old username-authorized recovery handlers were removed.
- Access inventory now fails503 on DB failure and includes folder-backed access paths. Scoped revoke preserves owner/folder permissions; direct shares/public links/group changes record atomic audit/activity. Group endpoints enforce actor/member/file-owner boundaries. Representative private queries measured7.07ms access and2.43ms timeline; these are fixture timings, not production SLOs.
- Trusted proxy parsing, generated request IDs, allowlisted request logs/error classes, completed/interrupted stream outcomes, bounded audit-on-cancel and privacy-safe support previews are covered. Apache's independent combined access log still records raw request paths/queries; backend log cleanup does not change it.
- Removed the service worker's cross-account file/folder cache interception. A real enabled-worker browser regression reproduced prior-account filenames and false200 on offline reads; the fix rejects failed network reads while preserving the existing owner-bound pending-action queue.
- First full private race gate failed only the old expectation of raw database diagnostic text. Updated it to require sanitized status/error-class evidence and reject raw driver errors; rerun73/73 passed. Independent review then found real onboarding repair persistence, recovery reload/cancel, PIN-envelope input, DB-error classification, hourly limiter purge and reset-version weaknesses. Focused repairs were made; a further review caught missing normal-onboarding version and cancellation racing a submitted reset, still under closure at this checkpoint.
- Post-first-review checkpoint: lint,469 frontend tests/96files/0skips, app+E2E types, harness configuration, private frontend/Go builds, Go vet and diff all exit0. Full private PostgreSQL race suite78 top-level tests/0skips passed. This checkpoint predates the final two browser-flow review repairs; final acceptance follows below.


## Iteration 7 — polish, verify and close (pending)

## Evidence and remaining gates
Raw logs and private build outputs: `.omx/reports/coherence-implementation-2026-09-14/`. Each iteration records exact commands, exit codes, results, fixes, changed files, lessons and next lens below. No global memory files or production data are changed. Five-person trials, physical devices/passkeys, reboot/restore and privileged backend deployment require separate evidence where unavailable.

Iteration 1 owned files: main.go; CI and trust-proof workflows; Playwright config/external runner/config verifier/package script; Access Center, Dashboard, Profile, Admin Tests, Help; mobile navigation, command palette, activity drawer; six new focused test files; this log. Unrelated starting dirt remains preserved.
